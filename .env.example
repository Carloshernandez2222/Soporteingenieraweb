# SQL Server (auth + tickets /api/casos/soporte)
SQLSERVER_HOST=127.0.0.1,1433
SQLSERVER_DATABASE=c
SQLSERVER_USER=sa
# En Codespace con docker-compose.yml use la misma contraseña:
SQLSERVER_PASSWORD=TrackAid_Secure2026!
# Opcional: URL completa para SQLModel (tiene prioridad si está definida)
# DATABASE_URL=mssql+pyodbc://...

# SQLite (taller + registro web/chatbot)
DATABASE_PATH=soporte.db

# CORS en producción (lista separada por comas)
# CORS_ORIGINS=https://tu-app.azurewebsites.net

# Usuarios demo (webmaster/soporte/usuario@trackaid.demo). Desactivar en producción si no los necesitas:
# SKIP_DB_SEED=1
