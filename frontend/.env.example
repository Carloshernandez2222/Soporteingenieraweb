# API del panel (tickets /casos /registrar). Vacío = mismo origen (proxy Vite en dev).
VITE_API_BASE=

# API TrackAid (auth). Vacío = rutas relativas /api/* vía proxy a :8000 en dev.
# En producción: https://tu-api.ejemplo.com
VITE_TRACKAID_API_URL=
